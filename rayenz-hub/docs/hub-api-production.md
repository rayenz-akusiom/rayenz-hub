# Hub API — production client configuration

Operator notes for Cognito, spend lock, invites, and cutover live in the source repo: [docs/hub-api-production.md](https://github.com/rayenz-akusiom/rayenz-hub/blob/main/docs/hub-api-production.md).

Sign in from the left nav (username `Rayenz` or an invitee). The API URL is baked into this Pages bundle at `npm run publish:hub`.
