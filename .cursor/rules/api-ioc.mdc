---
description: Inversify IoC conventions for packages/api handlers and services
globs: packages/api/**/*
alwaysApply: false
---

# API Inversion of Control (Inversify)

## Rules

- **Composition root only** binds dependencies: `packages/api/src/ioc/composition-root.ts`
- Handlers and services use `@injectable()`; dependencies via `@inject(TYPES.X)` or constructor injection
- Handlers receive `AppServices` (from `getAppServices()` or `createAppServices(overrides)` in tests)
- Handlers MUST NOT call `new Repository()`, `createDocClient()`, `readEnv()`, or `parseAuthContext()` directly
- Tests use `createTestServices()` / `createAppServices()` from `tests/api/helpers/test-services.ts`
- Register new services in the composition root before use in handlers

## Binding patterns

```typescript
// Symbols in types.ts
export const TYPES = {
  ApiEnv: Symbol.for('ApiEnv'),
  ProfileRepository: Symbol.for('ProfileRepository'),
  AuthService: Symbol.for('AuthService'),
};

// composition-root.ts
container.bind<ProfileRepository>(TYPES.ProfileRepository).to(ProfileRepository).inSingletonScope();
```

## Auth

Cognito JWT is the only request identity (`AuthService` + `CognitoAuthPort`). Do not branch on `process.env` inside handlers.
